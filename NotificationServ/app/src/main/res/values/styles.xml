<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyTheme" parent="android:Theme.Material">
       <item name="android:colorPrimary">@color/m1</item>
        <item name="android:colorPrimaryDark">@color/m2</item>
        <item name="android:colorAccent">@color/m3</item>
        <item name="android:windowBackground">@color/m4</item>
        <item name="android:textColor">@color/m1</item>
        <item name="android:navigationBarColor">@color/colorAccent</item>
    </style>





</resources>
